server:
  port: 8080
spring:
  data:
    mongodb:
      database: boards_db
  devtools:
    remote:
      restart:
        enabled: true
  mvc:
    view:
      prefix: /WEB-INF/views/

---

spring:
  profiles: test
